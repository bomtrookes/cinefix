<div class="container">
  <%= simple_form_for @watchlist do |f| %>
    <%= f.input :api_id, as: :hidden, input_html: { value: @movie.id } %>
    <%= f.button :submit, "Add to Watchlist", class: "btn btn-dark" %>
  <% end %>
</div>
